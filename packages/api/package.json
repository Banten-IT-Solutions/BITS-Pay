{
  "name": "@bits-pay/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "cf:config": "node ../../scripts/gen-wrangler.mjs api",
    "dev": "pnpm cf:config && wrangler dev",
    "build": "pnpm cf:config && wrangler deploy --dry-run",
    "deploy": "pnpm cf:config && wrangler deploy",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@bits-pay/shared": "workspace:*",
    "bcryptjs": "^3.0.3",
    "bits-qris": "^1.5.0",
    "hono": "^4.13.7",
    "jose": "^6.2.12",
    "qrcode": "^1.5.4",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260914.1",
    "@types/qrcode": "^1.5.6",
    "typescript": "6.0.3",
    "wrangler": "^4.129.0"
  }
}
